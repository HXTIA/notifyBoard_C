/**
 * 业务请求路径
 * ```
 *  🌰：/api/
 *  🌰：/uploadFile/
 * ```
 */
export enum BUSINESS_API_ROOT_PATH {
  API = '/api/'
}

/**
 * 业务域名（完整协议配置）
 * ```
 *  🌰：https://baidu.com:8080
 * ```
 */
export enum BUSINESS_DOMAIN {
  BUSINESS = ''
}

